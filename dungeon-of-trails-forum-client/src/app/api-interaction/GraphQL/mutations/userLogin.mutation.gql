mutation UserLogin($Email: String, $Password: String) {
  UserLogin(input: { Email: $Email, Password: $Password }) {
    accessToken
    user {
      id
      email
      isAdmin
      name
      avatarUrl
    }
  }
}
