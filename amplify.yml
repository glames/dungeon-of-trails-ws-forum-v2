version: 2
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npx nx build
  artifacts:
    baseDirectory: dungeon-of-trails-forum-client
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
