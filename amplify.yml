version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npx nx build
  artifacts:
    baseDirectory: dungeon-of-trails-forum-client/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
