version: 1
env:
  variables:
    key: production
frontend:
  buildpath:
  phases:
    preBuild:
      commands:
        - cd react-app
        - npm ci
    build:
      commands:
        - npx nx build
  artifacts:
    discard-paths: yes
    baseDirectory: ./dungeon-of-trails-forum-client/dist
  cache:
    paths:
      - path
      - path
